<Window x:Class="Forge.Studio.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonDock="https://github.com/Dirkster99/AvalonDock"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:models="clr-namespace:Forge.Studio.App.Models"
        Title="Forge Studio"
        Height="800"
        Width="1280"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New Project"/>
                <MenuItem Header="_Open .f11 Layout..."
                        Command="{Binding OpenFileCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit"/>
            </MenuItem>
            <MenuItem Header="_Edit"/>
            <MenuItem Header="_View"/>
            <MenuItem Header="_Build"/>
            <MenuItem Header="_Help"/>
        </Menu>

        <!-- Docking layout -->
        <avalonDock:DockingManager Grid.Row="1">
            <avalonDock:LayoutRoot>
                <avalonDock:LayoutPanel Orientation="Vertical">

                    <avalonDock:LayoutPanel Orientation="Horizontal">

                        <!-- LEFT: Solution Explorer + Toolbox -->
                        <avalonDock:LayoutAnchorablePane DockWidth="220">
                            <avalonDock:LayoutAnchorable Title="Solution Explorer"
                                    CanClose="False">
                                <TreeView ItemsSource="{Binding ActiveDocument.RootNodes}"
                                          SelectedItemChanged="TreeView_SelectedItemChanged">
                                    <TreeView.Resources>
                                        <HierarchicalDataTemplate DataType="{x:Type models:F11TreeNode}"
                                                                  ItemsSource="{Binding Children}">
                                            <TextBlock Text="{Binding DisplayName}"/>
                                        </HierarchicalDataTemplate>
                                    </TreeView.Resources>
                                </TreeView>
                            </avalonDock:LayoutAnchorable>

                            <avalonDock:LayoutAnchorable Title="Toolbox"
                                    CanClose="False">
                                <TextBlock Text="Toolbox (Forge11 widgets)"
                                        Margin="8"/>
                            </avalonDock:LayoutAnchorable>
                        </avalonDock:LayoutAnchorablePane>

                        <!-- CENTER: Designer + Code tabs -->
                        <avalonDock:LayoutDocumentPane>

                            <!-- DESIGNER tab -->
                            <avalonDock:LayoutDocument Title="Designer"
                                    CanClose="False">
                                <avalonDock:LayoutDocument.Content>
                                    <ContentControl DataContext="{Binding ActiveDocument}">
                                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                                      VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding Elements}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <Canvas Background="#1A1A24"
                                                                Width="{Binding CanvasWidth}"
                                                                Height="{Binding CanvasHeight}"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemContainerStyle>
                                                    <Style TargetType="ContentPresenter">
                                                        <Setter Property="Canvas.Left"
                                                                Value="{Binding X}"/>
                                                        <Setter Property="Canvas.Top"
                                                                Value="{Binding Y}"/>
                                                    </Style>
                                                </ItemsControl.ItemContainerStyle>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Width="{Binding Width}"
                                                                Height="{Binding Height}"
                                                                Background="{Binding Fill}"
                                                                BorderBrush="{Binding SelectionBorder}"
                                                                BorderThickness="2"
                                                                Cursor="Hand"
                                                                Tag="{Binding}"
                                                                MouseLeftButtonDown="DesignerElement_MouseLeftButtonDown">
                                                            <TextBlock Text="{Binding Label}"
                                                                       Foreground="White"
                                                                       FontSize="11"
                                                                       Margin="4,2"
                                                                       VerticalAlignment="Center"
                                                                       IsHitTestVisible="False"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </ContentControl>
                                </avalonDock:LayoutDocument.Content>
                            </avalonDock:LayoutDocument>

                            <!-- CODE tab -->
                            <avalonDock:LayoutDocument Title="Code"
                                    CanClose="False">
                                <avalonDock:LayoutDocument.Content>
                                    <ContentControl DataContext="{Binding ActiveDocument}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <avalonEdit:TextEditor
                                                Grid.Row="0"
                                                SyntaxHighlighting="XML"
                                                FontFamily="Consolas"
                                                FontSize="13"
                                                ShowLineNumbers="True"
                                                Background="#1E1E1E"
                                                Foreground="#D4D4D4"
                                                Loaded="XmlEditor_Loaded"/>

                                            <!-- Apply button -->
                                            <Button Grid.Row="1"
                                                    Content="Apply (reload canvas)"
                                                    Margin="4"
                                                    Padding="8,4"
                                                    HorizontalAlignment="Left"
                                                    Click="ApplyXml_Click"/>

                                            <!-- Error display -->
                                            <TextBlock Grid.Row="2"
                                                       Text="{Binding XmlError}"
                                                       Foreground="Red"
                                                       FontSize="11"
                                                       Margin="4,0,4,4"
                                                       TextWrapping="Wrap"/>
                                        </Grid>
                                    </ContentControl>
                                </avalonDock:LayoutDocument.Content>
                            </avalonDock:LayoutDocument>

                        </avalonDock:LayoutDocumentPane>

                        <!-- RIGHT: Properties -->
                        <avalonDock:LayoutAnchorablePane DockWidth="260">
                            <avalonDock:LayoutAnchorable Title="Properties"
                                    CanClose="False">
                                <ItemsControl ItemsSource="{Binding ActiveDocument.Properties}"
                                              Margin="4">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,3">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="90"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Key}"
                                                           FontWeight="SemiBold"
                                                           VerticalAlignment="Center"/>
                                                <TextBox Grid.Column="1"
                                                         Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                         VerticalAlignment="Center"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </avalonDock:LayoutAnchorable>
                        </avalonDock:LayoutAnchorablePane>

                    </avalonDock:LayoutPanel>

                    <!-- BOTTOM: Output + Error List -->
                    <avalonDock:LayoutAnchorablePane DockHeight="180">
                        <avalonDock:LayoutAnchorable Title="Output"
                                CanClose="False">
                            <TextBlock Text="Output"
                                    Margin="8"/>
                        </avalonDock:LayoutAnchorable>
                        <avalonDock:LayoutAnchorable Title="Error List"
                                CanClose="False">
                            <TextBlock Text="Error List"
                                    Margin="8"/>
                        </avalonDock:LayoutAnchorable>
                    </avalonDock:LayoutAnchorablePane>

                </avalonDock:LayoutPanel>
            </avalonDock:LayoutRoot>
        </avalonDock:DockingManager>

        <!-- Status bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>